1. `ask_stream` finally block appends turn even if `user_added=True` → double history entries — gate on `not user_added`
2. `_hard_cap_hit` never resets next day without restart → user blocked forever — fix in `_is_hard_cap_active_today`
3. `_goal_plan_jobs_inflight` only decremented on exception, not success → counter grows forever — move decrement outside `if exc`
4. `session._persist_session` called inside `session._lock` with disk I/O → main thread freezes — move persist call outside lock
5. `_summarize_history_background` calls LLM while holding `trimmer._lock` → deadlock — release lock before LLM call
6. Two concurrent `ensure_running` calls spawn duplicate OmniParser processes → port conflict crash — acquire `_proc_lock` at function entry
7. `shutdown` calls `_autonomy_execution_lock.release()` unconditionally when `acquire` returned False → ValueError crash — check acquire return value first
8. `background_plan` sets goal to `pending` without checking if already `cancelled` → cancelled goal reactivated — add cancelled status check
9. `json.dumps(result)` in `ask_stream` crashes on non-serializable tool output (bytes/datetime) → `[ERROR]` response, tool result lost — wrap in try/except with `str()` fallback
10. `_plan_goal` calls `engine.ask` up to 3 times after hard cap already hit — add cap check inside retry loop
11. `LocalMemoryStore.add` dedup check and insert not atomic → duplicate memories inserted under concurrency — hold `_insert_lock` across both operations
12. `switch_session` browser close holds lock with 1s timeout, on failure sets `_browser=None` anyway but skips `_browser_needs_reset` — stale browser reused next call
13. `export_session` two exports same second get identical filename, second overwrites first → data loss — always append nonce (already there but `safe_name` missing session_id uniqueness)
14. `_usage_alerted_day` never resets between days without restart → alert fires only once ever — compare against `date.today()` each call
15. `GoalRunner.close` races with `run` submit → `RuntimeError` propagates uncaught to autonomy loop — wrap submit in try/except RuntimeError
16. `_notify_telegram` inside `_interactive_confirm` can block executor thread if Telegram API is slow — add timeout to that notify call
17. `capture_screen_png` can return empty bytes, passed to `analyze_image` wasting Gemini API quota every interval — guard with `if not image_bytes: return`
18. `_sync_all_inflight` stays True forever if executor submit raises → memory sync permanently disabled — reset flag in except block
19. `run_voice_loop` spawns new capture thread each VAD timeout without joining previous → audio device exhaustion/freeze — join previous thread before starting new
20. `background_plan` in `_resume_goal` never decrements `_goal_plan_jobs_inflight` → counter grows forever — add finally block with decrement