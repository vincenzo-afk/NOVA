[CRITICAL] | main.py:background_plan | Cancelled goal re-appears as pending | User cancels goal while planner completes; `background_plan` acquires `_goal_lock` and sets `item["status"] = "pending"` with no check for `"cancelled"` | Add `if item.get("status") == "cancelled": break` before setting pending

[CRITICAL] | main.py:ask_stream | Every message adds up to 1.5s latency; offline users always blocked | `NetworkState.is_online()` opens TCP socket to `1.1.1.1:53` with 1.5s timeout on every single `ask_stream` call in the hot path | Cache network state with a short TTL instead of probing per message

[MAJOR] | main.py:_interactive_confirm | All concurrent requests freeze while waiting for terminal input | `input(prompt)` called from `dispatcher.execute` → called from `ask_stream` generator; blocks the iterating thread including Telegram bot's async loop | Move confirm to background thread or always return False when not `isatty()`

[MAJOR] | main.py:ask_stream | Tool result silently lost; user sees `[ERROR] Generation failed` | `json.dumps(result, ensure_ascii=False)` raises `TypeError` if any tool returns non-serializable data (bytes, datetime, custom objects); caught by outer `except Exception` which yields error instead | Wrap `json.dumps` in try/except with fallback `str(result)`

[MAJOR] | interfaces/voice_interface.py:run_voice_loop | Audio device locked; process accumulates hung threads | On VAD capture timeout, `continue` restarts loop and spawns new `_capture_job` thread without joining or cancelling the previous hung thread; repeats each timeout | Set a stop flag on `capture_result` dict and join the previous thread before starting a new one

[MAJOR] | vision/omniparser_server.py:ensure_running | OmniParser double-starts or proc reference corrupted | `ensure_running` (daemon thread at startup) and `restart` (health monitor thread) both modify `self.proc` and `self.log_file` with no lock; concurrent calls produce two `Popen` subprocesses | Add `threading.Lock()` to `OmniParserServer` guarding `self.proc` mutations

[MAJOR] | main.py:_plan_goal | Daily token cap blown 3× during goal planning | `engine.ask()` called up to 3 times in retry loop; `_hard_cap_hit` checked only at `ask_stream` entry, not inside `_plan_goal`; all 3 retry calls execute even if cap is hit after attempt 1 | Check `_hard_cap_hit` under `_usage_lock` before each `engine.ask()` attempt

[MAJOR] | web/scraper.py:_validate_url | Thread count grows unbounded on repeated scrape timeouts | `ThreadPoolExecutor(max_workers=1)` created per `scrape_text()` call; on DNS timeout, `fut.cancel()` + `pool.shutdown(wait=False)` leaves the DNS resolution thread running; each timeout leaks a thread | Reuse a module-level executor or use `socket.setdefaulttimeout` directly

[MAJOR] | core/memory/memory_router.py:sync_pending | Memory items synced but still retried next time | `synced_ids = {id(item) for item in synced}` compares Python object identity; after `list(...)` snapshot and re-list of `current_items`, objects are the same refs so this works — but if `add()` appended new dicts between snapshot and re-list with same content, `id()` won't match new objects | Use content hash instead of `id()` for removal

[MAJOR] | main.py:shutdown | NOVA hangs indefinitely on exit if a tool is running | `goal_runner.close()` calls `executor.shutdown(wait=True)` with no timeout; if `dispatcher.execute` is blocked inside a hanging tool (browser, subprocess), shutdown never completes | Use `executor.shutdown(wait=True, cancel_futures=True)` with a watchdog timeout thread

[MAJOR] | core/context/environment.py:_get_clipboard | Background refresh thread can hang indefinitely | `_get_clipboard()` calls `subprocess.check_output(["xclip", ...])` with no timeout; if `xclip` hangs (common on headless Linux), `_refresh_background` thread blocks forever holding `_ENVIRONMENT_LOCK` equivalent and no further refreshes occur | Add `timeout=2` to `subprocess.check_output`

[MINOR] | main.py:NOVAApp.__init__ | Manual `goal.run` tool calls not serialized as intended | `self._goal_execution_lock = threading.Semaphore(1)` assigned but never `acquire()`d anywhere in the codebase; concurrent `goal.run` tool calls execute in parallel without the intended serialization | Either remove the unused semaphore or actually use it in the manual goal execution path

[MINOR] | main.py:_start_health_monitoring | Health monitor busily restarts screen_watcher on headless system | `screen_watcher.restart()` registered as `restart_fn`; on headless system `start()` raises each time → health monitor marks as down → retries every `HEALTH_MONITOR_INTERVAL` with `max_restart_attempts=3` then marks `restart_failed` permanently | Guard `screen_watcher.start()` with display availability check before registering subsystem

[MINOR] | main.py:_resume_goal | `_goal_plan_jobs_inflight` leaks on planning failure in resume path | In `_resume_goal`, `background_plan` defined inline increments counter inside body (not `finally`), but the `_on_done` callback only decrements on `fut.exception()` not on normal completion — causing asymmetry vs `_add_goal`'s pattern if any unhandled exception exits `background_plan` mid-run | Wrap counter decrement in `finally` block inside `background_plan`

[MINOR] | core/llm/engine.py:_cloud_stream | Streaming response connection never explicitly closed on mid-stream exception | `response.iter_lines()` raises `ConnectionError`; `response.close()` never called; TCP socket held open until GC finalizes the response object | Wrap `iter_lines` loop in `try/finally: response.close()`