Critical
PEP 479 StopIteration silently swallowed in ask_stream
Bugs
main.py:~820
▶
Problem
In Python 3.7+, a StopIteration raised inside a generator is converted to RuntimeError by PEP 479. The except StopIteration block after next(stream) will never fire, so the assistant turn is never committed and usage is never tracked on an empty stream.
Root cause
PEP 479 changed generator semantics; the code was written assuming pre-3.7 behavior.
Fix
Replace with: try: first_token = next(stream) ... except RuntimeError as e: if "StopIteration" in str(e): ... — or use a sentinel pattern with a flag variable instead of catching StopIteration.
Priority
High
Critical
ask_stream generator abandonment causes data loss
Bugs
main.py:ask_stream
▶
Problem
If a caller (Telegram, GUI, voice) abandons the generator mid-stream (user disconnects, timeout, exception in caller), _track_usage, session.add_turn, and memory.add are never called. The conversation turn is lost permanently with no indication.
Root cause
Python generators only execute cleanup code when exhausted or GC-collected; there is no finally block in ask_stream to flush state.
Fix
Wrap the body in a try/finally that records whatever partial output exists. Or accumulate tokens in the caller and call a commit method afterward.
Priority
High
Critical
Plugin sandbox allows data exfiltration via unblocked modules
Security
core/plugin_loader.py
▶
Problem
_BLOCKED_IMPORTS blocks subprocess, os, socket etc., but requests, ssl, http.client, ftplib, smtplib, urllib are NOT blocked. A malicious plugin can import requests and exfiltrate any data, credentials, or memory contents. open() is blocked in safe_builtins but pathlib.Path, io, and tempfile are reachable through unblocked imports.
Root cause
The allowlist approach is incomplete — it is nearly impossible to enumerate all dangerous modules.
Fix
Use RestrictedPython or a subprocess-isolated plugin process with IPC instead of exec() in the host process. At minimum, add requests, urllib, http, ssl, ftplib, smtplib, pathlib, io, tempfile to the blocked list.
Priority
High
Critical
Registry allowlist check vulnerable to path traversal
Security
safety/guardrails.py:check()
▶
Problem
The registry allowlist uses path.startswith("hkey_current_user\\software\\nova"). A path like HKEY_CURRENT_USER\SOFTWARE\NOVA\..\..\Windows\CurrentVersion\Run passes the startswith check after lowercasing but before normalization, allowing write to startup registry keys.
Root cause
No path normalization is applied before the allowlist comparison.
Fix
Normalize the path by resolving ".." segments (os.path.normpath equivalent for registry paths) and re-validate after normalization. Also reject any path containing "..".
Priority
High
Critical
Autonomy runner confirm_callback race condition — permanent always-approve
Risk
main.py:_autonomy_loop
▶
Problem
When a goal is approved for one step, confirm_callback is set to lambda p: True. If an exception occurs between setting and restoring the callback, all future high-risk autonomy steps are permanently auto-approved without user confirmation.
Root cause
The callback restoration is not inside a try/finally block.
Fix
Use try/finally to guarantee callback restoration: try: self.autonomy_runner.confirm_callback = lambda p: True; result = ...; finally: self.autonomy_runner.confirm_callback = self._autonomy_confirm_callback
Priority
High
Major
_goal_plan_executor.shutdown(wait=True) can block forever on shutdown
Bugs
main.py:shutdown()
▶
Problem
The shutdown() method calls _goal_plan_executor.shutdown(wait=True). If a goal planning LLM call is in progress (up to 90s timeout), shutdown blocks indefinitely. This hangs SIGTERM handling and makes containerized graceful shutdown impossible.
Root cause
The LLM call inside background_plan has no cancellation mechanism.
Fix
Set wait=False or add a cancel_futures=True parameter (Python 3.9+). Alternatively, set a short timeout and then abandon: executor.shutdown(wait=False, cancel_futures=True).
Priority
High
Major
Browser instance not thread-safe — concurrent requests corrupt Playwright state
Bugs
main.py:_get_browser()
▶
Problem
_get_browser() returns a shared Browser instance with no locking. Playwright sync API is NOT thread-safe. Concurrent browser.open() + browser.extract_text() calls from Telegram and GUI threads simultaneously cause crashes or undefined state.
Root cause
Lazy singleton pattern with no mutex; Playwright documents that sync API is single-threaded.
Fix
Add a threading.Lock() around all browser operations, or create a separate Browser instance per session, or route all browser calls through a single dedicated thread with a queue.
Priority
High
Major
switch_session calls _browser_close() without checking concurrent usage
Bugs
main.py:switch_session()
▶
Problem
switch_session() sets self._browser = None after closing. If another thread is mid-execution inside _get_browser().open(), it now holds a reference to a closed browser object. Subsequent calls create a new instance, but the in-flight call continues on a closed browser, raising PlaywrightError.
Root cause
No synchronization between session switching and browser usage.
Fix
Use a per-browser lock; acquire it in switch_session before closing, and in _get_browser() before any usage.
Priority
Medium
Major
DNS rebinding TOCTOU in SSRF protection
Security
web/scraper.py:_validate_url()
▶
Problem
_validate_url() resolves hostname to IP and checks for private ranges, then the actual requests.get() resolves DNS again. A DNS rebinding attack can serve a public IP during the check and a private IP (e.g. 169.254.169.254 AWS metadata) for the real request, fully bypassing SSRF protection.
Root cause
DNS resolution is inherently racy; two lookups at different times can return different IPs.
Fix
Resolve DNS once, connect to the resolved IP directly (bypass DNS for the actual HTTP call), or use a library like ssrf_py that handles this correctly.
Priority
High
Major
socket.getaddrinfo() has no timeout — request handlers can hang indefinitely
Security
web/scraper.py:_validate_url()
▶
Problem
socket.getaddrinfo(hostname, None) has no timeout parameter. A slow or malicious DNS server can make any web.scrape or web.crawl tool call hang forever, blocking the calling thread permanently.
Root cause
Python socket module does not support per-call timeouts on getaddrinfo.
Fix
Wrap in a concurrent.futures.ThreadPoolExecutor with a 5s timeout, or use dnspython with explicit timeouts: dns.resolver.resolve(hostname, lifetime=5.0).
Priority
High
Major
GoalRunner ignores dry_run flag for guardrails medium-risk countdown
Bugs
tasks/goals.py:run()
▶
Problem
Inside run(), guardrails check runs only if not dry_run. However when dry_run=True, the dispatcher.execute() is still called with dry_run=True from the dispatcher side. The asymmetry means dry-run for the plan stage misses guardrail evaluation, giving an inaccurate dry-run report.
Root cause
dry_run semantics are split between GoalRunner and Dispatcher without a unified contract.
Fix
Pass dry_run into the guardrails.authorize() call as well, so the dry-run report includes what would have been prompted/blocked.
Priority
Medium
Major
doc.ingest allows arbitrary file system path traversal
Security
main.py:DocIngestArgs
▶
Problem
DocIngestArgs has no path validation. An LLM instructed or manipulated by a document can call doc.ingest with filepath="/etc/passwd" or "C:\Windows\System32\config\SAM" and the contents will be embedded into the vector store and returned in RAG results.
Root cause
No allowlist or path restriction on ingested file paths.
Fix
Restrict ingestable paths to a configurable directory (e.g. ~/nova_docs/). Reject any absolute paths outside the allowed directory and reject paths containing "..".
Priority
Medium
Major
No file size limit in DocumentLoader — OOM on large files
Missing
rag/doc_loader.py:load()
▶
Problem
DocumentLoader.load() reads entire files into memory with no size check. Ingesting a multi-GB PDF or text file will exhaust memory and OOM-kill the process. There is also no timeout for PDF extraction.
Root cause
Missing guard in the ingestion pipeline.
Fix
Add a size check before loading: if path.stat().st_size > MAX_DOC_SIZE_BYTES: raise ValueError. Set a reasonable default (e.g. 50 MB). For PDFs, use page-by-page streaming.
Priority
High
Major
Goal step progress lost on mid-execution crash — cursor only updated after full run
Risk
main.py:_autonomy_loop
▶
Problem
The cursor (which step we are on) is only updated in _goals after runner.run() completes. If the process crashes mid-execution (10 steps done out of 20), all 10 completed steps will re-execute on restart because cursor never advanced. This can cause duplicate actions (duplicate emails sent, files deleted twice, etc.).
Root cause
Persistence is coarse-grained at the goal level, not step level.
Fix
Update cursor and _persist_goals() after each individual step completes inside a custom GoalRunner subclass or by using a callback passed into runner.run().
Priority
High
Major
UsageTracker._daily grows without bound — long-running process memory leak
Performance
utils/usage_tracker.py
▶
Problem
_daily is a defaultdict of dicts keyed by date. It is never pruned. A long-running process (weeks/months) accumulates thousands of date entries in RAM. Each entry also includes full session history dictionaries.
Root cause
No TTL or cleanup policy on historical usage data.
Fix
In add(), prune entries older than 8 days: self._daily = {k:v for k,v in self._daily.items() if (date.today()-k).days <= 7}
Priority
Medium
Major
Telegram bot has no graceful shutdown hook — run_polling() blocks forever
Risk
interfaces/telegram_bot.py:run_telegram_bot()
▶
Problem
app.run_polling() is a blocking call with no shutdown mechanism. When shutdown() is called on NOVAApp, the Telegram thread keeps polling indefinitely. The process cannot exit cleanly when run in Telegram mode.
Root cause
python-telegram-bot requires idle() or a shutdown signal; neither is wired.
Fix
Run run_polling() with stop_signals=() and capture signal separately, or call app.stop() and app.shutdown() from the NOVAApp.shutdown() method.
Priority
Medium
Major
Ollama health check triggers spurious restarts during intentional startup delay
Connectivity
main.py:_check_llm_engine()
▶
Problem
The health monitor calls _check_llm_engine() which fires immediately on start. If Ollama takes >60s to load a large model, the health monitor marks it "down" and calls _restart_llm_engine() (which launches another ollama serve), creating duplicate Ollama processes.
Root cause
No startup grace period in the health check registration.
Fix
Add a startup_delay parameter to register_subsystem() that skips checks for the first N seconds. Alternatively, skip the Ollama restart if a process is already running (check with psutil).
Priority
Medium
Major
_load_session silently discards corrupted session files
Bugs
core/session.py:_load_session()
▶
Problem
If a session JSON file is corrupted (truncated write, disk error), _load_session catches all exceptions and returns None, creating a fresh empty session. The user loses their entire conversation history with no warning.
Root cause
Overly broad exception handling; no attempt to recover partial data or warn the user.
Fix
Log a warning with the exception details. Attempt to read a backup file (keep last 2 writes). Raise a specific exception that the caller can present to the user before silently resetting.
Priority
Medium
Major
MCP HTTP authorization header injected over plain HTTP to non-local hosts
Security
main.py:_connect_mcp()
▶
Problem
The code checks parsed_scheme == "http" and not is_local, but is_local only matches exact strings {"localhost","127.0.0.1","::1"}. Custom /etc/hosts entries (e.g. "internal-api" mapped to 10.0.0.1) bypass this check and receive the Authorization Bearer token over unencrypted HTTP.
Root cause
Hostname check is string-based, not IP-based.
Fix
Resolve the hostname to an IP and check if it is a private/internal address using the same _is_private_ip logic already in web/scraper.py.
Priority
Medium
Major
_summarize_history_background submits new jobs on every ask_stream call
Performance
main.py:_context_messages()
▶
Problem
The snippet hash check (_last_snippet_hash) prevents duplicate background summarization, but this is a single shared variable — not per-session. In a multi-session environment, session A and B both overwrite _last_snippet_hash, causing missed summaries. Also, the ThreadPoolExecutor queue is unbounded.
Root cause
Single _last_snippet_hash shared across all sessions; no per-session tracking.
Fix
Use a dict keyed by session_id for _last_snippet_hash. Add a bounded queue (max_workers=1 with bounded queue) to prevent memory growth.
Priority
Medium
Major
requirements.txt and requirements.lock are identical — Dockerfile uses wrong file
Deployment
Dockerfile / requirements.lock
▶
Problem
Both files are currently identical. The Dockerfile uses requirements.lock while setup.py installs from requirements.txt. If they diverge (likely as the project evolves), Docker builds and local installs will silently have different dependency trees, causing hard-to-diagnose production bugs.
Root cause
No tooling (pip-compile, pip-tools) is set up to generate requirements.lock from requirements.in.
Fix
Use pip-tools: create requirements.in with loose pins, generate requirements.txt (local dev) and requirements.lock (pinned production) via pip-compile. Add a CI check that verifies they are in sync.
Priority
High
Major
docker-compose.yml uses :latest tags — non-reproducible builds
Deployment
docker-compose.yml
▶
Problem
ollama/ollama:latest and chromadb/chroma:latest will pull different versions on each docker compose pull. A breaking API change in either service will silently break the application in production with no version history to roll back to.
Root cause
Convenience over reproducibility.
Fix
Pin to specific versions: ollama/ollama:0.5.4, chromadb/chroma:0.6.3 (or current stable). Add a monthly bot to bump versions with testing.
Priority
High
Minor
snapshot_environment network status stale for up to 10 seconds
Bugs
core/context/environment.py
▶
Problem
The environment cache TTL is 10 seconds. If the user goes offline and asks a question within 10s, the LLM context reports "network: online" while actually offline. This causes the LLM to plan cloud-dependent actions that will fail.
Root cause
Aggressive caching without a way to invalidate on network state change.
Fix
Either exclude the network field from caching (query it fresh each call), or register a network-change callback that invalidates _ENVIRONMENT_CACHE_TIME = 0.
Priority
Medium
Minor
CLI PIN file path exposed via environment variable
Security
interfaces/cli.py:run_cli()
▶
Problem
CLI_PIN_FILE environment variable containing the path to the PIN file is visible in /proc/PID/environ and ps output. The PIN file itself has no permissions check — if .jarvis/ is world-readable, any local user can read the PIN.
Root cause
PIN path stored in environment rather than a config file with strict permissions.
Fix
Use a hardcoded default path only (~/.config/nova/cli_pin), set 600 permissions on creation (os.chmod), and never expose the path via an environment variable.
Priority
Medium
Minor
Session history list has no maximum size bound
Performance
core/session.py / core/memory/context_trimmer.py
▶
Problem
session.history grows indefinitely. The context trimmer only trims what is sent to the LLM — it does not prune session.history itself. A session with 10,000 turns has a 10,000-element list that is scanned on every add_turn call and serialized fully on every persist.
Root cause
No ring buffer or maximum history length enforced at the session level.
Fix
Cap history at a configurable MAX_HISTORY_TURNS (e.g. 500). When exceeded, drop oldest turns that are already covered by the rolling summary.
Priority
Low
Minor
Goal planning LLM calls not tracked in usage accounting
Missing
main.py:_plan_goal()
▶
Problem
_plan_goal() calls self.engine.ask() up to 3 times for planning retries, and _track_usage is called with "[goal_plan]" as user_text. However, the system prompt token count uses the planner_prompt (short) but the actual tokens sent include the full tool schema injected by build_system_prompt. This systematically undercounts planning token usage.
Root cause
build_system_prompt appends the full dispatcher schema but _track_usage only measures planner_prompt.
Fix
Pass the full system_prompt string (after build_system_prompt) into _track_usage, not just the base planner_prompt.
Priority
Medium
Minor
No web search rate limiting — DuckDuckGo IP ban risk
Missing
web/search.py
▶
Problem
The web.search tool has no rate limiting. An LLM in agentic mode (especially in the autonomy loop) can fire dozens of searches per minute, triggering DuckDuckGo IP bans. There is also no per-session or per-day search quota.
Root cause
No rate limiting layer between the dispatcher and the search function.
Fix
Add a token bucket rate limiter (e.g. 10 searches/minute) in the web.search wrapper in main.py, separate from the dispatcher-level rate limit.
Priority
Medium
Minor
openai==1.3.5 and google-generativeai==0.3.0 are severely outdated
Deployment
requirements.txt
▶
Problem
openai==1.3.5 is from late 2023 (current is ~1.35). It lacks structured outputs, response format changes, and has known bugs. google-generativeai==0.3.0 predates the Gemini 1.5 and 2.0 model APIs — GEMINI_TTS_MODEL="gemini-2.5-flash-preview-tts" will fail with this SDK version.
Root cause
Versions were pinned but never updated.
Fix
Update to openai>=1.30.0 and google-generativeai>=0.8.0. Run the test suite after updating. The Gemini TTS feature is broken until the SDK is updated.
Priority
Medium
Minor
Health monitor restart_failed state is permanent — requires process restart to recover
Risk
core/health.py:poll_once()
▶
Problem
Once a subsystem reaches restart_failed (after max_restart_attempts=3 consecutive failures), _restart_attempts[name] is never reset unless healthy becomes True. If the external dependency recovers on its own (Ollama restarted manually), the health monitor still shows restart_failed and never attempts recovery again.
Root cause
_restart_attempts is only reset on healthy=True, but restart_failed status means the code path for check_fn is still executed. If it returns True, it would recover — but the code does reset on healthy. Wait, let me re-read... Actually on healthy=True the counter resets. So if Ollama comes back on its own, the next poll sees healthy=True and resets. This is actually OK. But if restart_failed blocks the restart_fn from being called, future healthy checks still happen — so self-recovery IS possible. This is a minor doc issue, not a hard bug.
Fix
Document this recovery behavior. Add a /health force-reset CLI command that clears restart_failed status for a named subsystem.
Priority
Medium
Minor
mem0 client SDK variant probing produces no warning on total failure
Connectivity
core/memory/mem0_client.py:_init_remote()
▶
Problem
_init_remote() tries 4 different module/class combinations silently. If all fail (wrong SDK version installed), self._remote_enabled stays False and all mem0 calls silently fall back to the local in-memory list with no log message or health alert.
Root cause
Exception handling is too broad; all SDK import errors are swallowed.
Fix
Log a warning after all variants fail: logger.warning("mem0 remote client failed to initialize — falling back to local memory. Check mem0ai SDK version."). Register mem0 connectivity in the health monitor.
Priority
Low
Minor
Emergency stop file in home directory fails in read-only containerized environments
Security
safety/guardrails.py
▶
Problem
_EMERGENCY_STOP_FILE = Path.home() / ".jarvis" / "emergency_stop". In Docker with a read-only home directory or no home directory, this silently fails. The fallback to .cwd()/.jarvis/ works but is not mounted as a volume in docker-compose.yml, so the flag is lost on container restart.
Root cause
No volume mount for the .jarvis/ directory in docker-compose.
Fix
Add ./.jarvis:/app/.jarvis volume mount in docker-compose.yml. Also log a warning if both paths fail to write (currently the fallback silently discards the exception).
Priority
Low
Minor
exports/ directory grows without bound — no rotation or cleanup
Missing
utils/exporter.py
▶
Problem
Every session export creates a new timestamped file. In long-running deployments with frequent exports (Telegram /export commands, auto-exports on context reset), the exports/ directory accumulates gigabytes of markdown files with no cleanup.
Root cause
No retention policy implemented.
Fix
After writing a new export, keep only the last N files per session name (e.g. N=10). Implement in export_json/export_markdown or as a daily scheduler job.
Priority
Low
Minor
ADB send_sms allowlist check duplicated in two places with different error paths
Missing
main.py:_adb_send_sms() / control/adb/adb_client.py:send_sms()
▶
Problem
The phone number allowlist is checked in both _adb_send_sms (returns error string) and ADBClient.send_sms (raises ValueError). The dispatcher calls _adb_send_sms which returns an error string — that error string is returned as a tool result, not as a blocked/error status, so the LLM may not understand the action was blocked.
Root cause
Inconsistent error handling between wrapper and implementation.
Fix
Remove the duplicate check from ADBClient.send_sms (the dispatcher-level check in _adb_send_sms is sufficient). Return a structured dict with status: "blocked" from _adb_send_sms instead of a plain error string.
Priority
Low
Minor
OmniParser client auth token not updated after server restart
Performance
main.py:_get_omniparser_client()
▶
Problem
When OmniParser restarts (via health monitor restart_fn), a new auth_token is generated in OmniParserServer.__init__ only — the token is fixed at startup. The existing _omniparser_client gets the new token via _get_omniparser_client() which updates it. But the OmniParser server subprocess is started with the original token from the initial command. After restart, the client sends the new token but the server expects the original token, causing 401 errors.
Root cause
auth_token is embedded in the subprocess command at construction time and never regenerated on restart.
Fix
Either use a fixed auth token from settings (generated once and stored in .env), or regenerate the command in restart() with a new token and update self.auth_token before restarting.
Priority
Low
Minor
pyproject.toml pythonpath only adds "." — sub-package imports may fail in some test runners
Deployment
pyproject.toml
▶
Problem
pythonpath = ["."] is correct for pytest but conftest.py also manually inserts ROOT into sys.path. The duplication is harmless but conftest.py uses __file__.resolve().parents[1] which is tests/../ = ROOT, correct. However if tests are run from a subdirectory, the pythonpath setting may not match conftest.py expectations.
Root cause
Redundant sys.path manipulation.
Fix
Remove the manual sys.path.insert in conftest.py and rely solely on pyproject.toml pythonpath = ["."].
Priority
Low
Minor
No input length validation on tool arguments — LLM can pass unbounded strings
Missing
main.py:various Args models
▶
Problem
Pydantic models like FileWriteArgs, KeyboardTypeArgs, BrowserFillArgs have no max_length constraints. An LLM could generate a 100MB string as content for win32_api.write or as keyboard input, causing disk exhaustion, memory spikes, or process hangs.
Root cause
Pydantic models validate types but not sizes.
Fix
Add Field(max_length=...) to all string fields in tool arg models. Use sensible limits: file content 10MB, keyboard text 10000 chars, search queries 500 chars.
Priority
Medium
Minor
launch_process shlex.split with posix=False on Windows may still be injection-vulnerable
Security
control/win32_api.py:launch_process()
▶
Problem
shlex.split(command, posix=False) on Windows does not handle all Windows-specific quoting edge cases. Commands with embedded null bytes, COM object invocations, or PowerShell pipeline characters (|, &) can still cause unexpected behavior when passed to subprocess.Popen.
Root cause
shlex is designed for POSIX; Windows quoting rules are different.
Fix
On Windows, use subprocess.Popen(command, shell=False) with an explicit list of arguments, or validate the command against an allowlist of permitted executables before splitting.
Priority
Low
Minor
ADBClient.type_text does not escape all ADB special characters
Bugs
control/adb/adb_client.py:type_text()
▶
Problem
The escaping in type_text replaces spaces with %s and escapes some shell chars. However, % itself is not escaped, so text containing "100%" becomes "100%s" after space-replacement interacts with percent. Additionally, Unicode characters above U+00FF are silently dropped by ADB input command on some Android versions.
Root cause
Incomplete character escaping for ADB input protocol.
Fix
Use ADB key events for special chars, or URL-encode the full input string and decode on device. At minimum, escape % before replacing spaces.
Priority
Low
Minor
No /health or /metrics HTTP endpoint for containerized deployment monitoring
Missing
main.py
▶
Problem
The Docker HEALTHCHECK uses ps aux | grep, which only verifies the process is alive, not that it is actually responding to requests. There is no HTTP endpoint for Kubernetes liveness/readiness probes or Prometheus scraping.
Root cause
No lightweight HTTP server embedded in the main process.
Fix
Add a minimal HTTP health server (Python http.server in a daemon thread) on port 8080 that returns 200 {"status":"ok", "emergency_stop":false, "subsystems": ...} from health.status_table().
Priority
Low
Minor
jarvis_jobs.sqlite path uses __file__ resolution — breaks when installed as a package
Deployment
tasks/scheduler.py
▶
Problem
_DEFAULT_DB = f"sqlite:///{Path(__file__).resolve().parent.parent}/jarvis_jobs.sqlite" puts the database in the project source tree. This is correct for development but breaks if the package is installed (pip install -e .) because __file__ points inside site-packages.
Root cause
Database path coupled to source tree location.
Fix
Use Path.home() / ".nova" / "jarvis_jobs.sqlite" or an XDG-compliant path, and make it overridable via NOVA_DATA_DIR env var.
Priority
Low