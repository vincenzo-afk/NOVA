[MAJOR] | main.py:_context_messages | User's question answered with wrong session's history and memories | `NetworkContextDetector` background thread calls `switch_session()` after `ask_stream` captures `current_session` but before `_context_messages` runs; `_context_messages` re-reads `self.session.current` independently, returning history from the new session while `ask_stream` appends the answer to the old session | Pass the already-captured `session_id` and `history` into `_context_messages` instead of re-reading `self.session.current` inside it

[MAJOR] | core/session.py:add_turn | Proactive alert or autonomy completion message written to wrong session history after session switch | `with self._current._lock:` acquires lock on the SessionState at that instant, but `self._current.history.append` re-evaluates `self._current` inside the block; if `switch_session()` runs on the NetworkContextDetector thread between these two bytecodes, the append goes to the new session's history while holding the old session's lock | Capture `session = self._current` as a local before the `with` block and use only `session._lock` and `session.history` inside it

[MAJOR] | main.py:switch_session | Browser object leaked when lock times out during session switch | When `self._browser_lock.acquire(timeout=1.0)` returns False, the else-branch does `self._browser = None` without the lock; a concurrent `_browser_open` thread holding the lock may have just created and stored a new browser in `self._browser`, which is then orphaned (never closed) | In the else-branch, log the lock-timeout warning only; never null `self._browser` without the lock

[MAJOR] | interfaces/telegram_bot.py:run_telegram_bot | Telegram bot permanently stops responding after the first managed-lifecycle start when python-telegram-bot ≥ v20 is installed | `app.initialize()` is a coroutine in PTB v20+ but called synchronously without `await`; the `_poll` thread catches the resulting crash with bare `except Exception: pass` and exits silently; polling never restarts | Check PTB version and use `asyncio.run(app.initialize())` or restructure as a fully async function; add error logging that surfaces the crash

[MINOR] | main.py:ask_stream | Unpaired user message permanently pollutes future LLM context for that session | When Ollama or any backend returns a completely empty stream, `next(stream)` raises `StopIteration`; user turn is appended in the except block; `assistant_text` is `""`; `if assistant_text:` guard skips appending the assistant turn; the `finally` block hits `if not partial: return` and exits without writing an assistant reply | In the `StopIteration` / empty-stream case, write a placeholder assistant turn such as `"(no response)"` so history stays balanced

[MINOR] | main.py:export_session | Second export silently overwrites first when called twice within the same clock-second for the same session | Filename is `f"exports/{safe_name}_{int(time.time())}.md"`; `int(time.time())` is identical for calls within the same second; the second `Path.write_text` overwrites the first with no error | Append a monotonic sub-second counter or `uuid.uuid4().hex[:6]` to the timestamp component

[MINOR] | voice/tts_offline.py:_ensure_worker | Two concurrent first calls to `speak()` launch two TTS worker threads sharing one queue, driving pyttsx3 from two threads simultaneously | `_ensure_worker` has no lock; two threads can both evaluate `if _WORKER_THREAD is None or not _WORKER_THREAD.is_alive()` as True, both call `threading.Thread.start()`, both assign to `_WORKER_THREAD`; the orphaned first thread and the stored second thread both pull from `_QUEUE`; pyttsx3 engine state is undefined across threads | Guard `_ensure_worker` with a module-level `threading.Lock` wrapping the entire `_QUEUE` / `_WORKER_THREAD` initialization block

[MINOR] | main.py:_auto_document_goal_outcome | Goal completion note stored in wrong session's memory after a session switch mid-goal | Method reads `self.session.current.session_id` at completion time; if the user switched sessions after the goal started (e.g., via `/session work`), the outcome note is written to the post-switch session's memory | Capture the session_id at goal-start time and pass it through to `_auto_document_goal_outcome`