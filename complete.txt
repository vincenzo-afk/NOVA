Fix verification log (current codebase)

Resolved / clarified items:

- Bug 22 (web/scraper.py IPv6 rewrite): resolved.
  _validate_url() can return IPv6 addresses; scrape_text() now uses _format_host_for_netloc() to bracket IPv6 literals (RFC-compatible), and web/crawler.py continues to rely on _validate_url() for shared semantics.

- Bug 34 (_autonomy_loop goals_snapshot / _goal_lock): resolved.
  main.py uses id-based selection while holding _goal_lock and no longer relies on index-based lookup from a deepcopy snapshot.

- Bug 32 (BackgroundScheduler / list_jobs_detailed): no actionable bug.
  TaskScheduler.remove_job() already handles missing jobs by returning False on exception (including JobLookupError), and list_jobs_detailed() already guards next_run_time before calling isoformat().

- Bug 37 (CommitmentExtractor / extract_commitments / _commit_memory_async): no actionable bug.
  _commit_memory_async intentionally calls extract_commitments(..., role="user") on the user_text path only. CommitmentExtractor role filtering is therefore consistent with the caller contract.

Additional concurrency fixes applied now:

- GoalTemplateLibrary _save() now holds _lock through payload generation and file write, serializing concurrent saves.
- BehaviorModel save path now snapshots _data while holding _lock, then writes snapshot outside the lock.
- _resume_goal() done-callback decrements _goal_plan_jobs_inflight only when future.exception() is non-None.
- RoundRobinPool logging no longer depends on utils.logger/get_logger during warning path; it uses stdlib logging.
