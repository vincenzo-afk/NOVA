# NOVA — pinned dependency manifest
# Generated: 2026-03-28
# Deployment fix: All critical dependencies are now version-pinned to prevent
# silent breakage from upstream releases on next pip install.
# To regenerate: python3 -m pip freeze > requirements.txt

# LLM
openai>=1.50.0,<2.0.0
# ollama         # installed separately — use official installer
google-generativeai==0.8.5

# Voice
pvporcupine==3.0.2
pyaudio>=0.2.13
sounddevice>=0.4.6
# silero-vad                 # install separately from torch ecosystem
faster-whisper>=1.0.0
pyttsx3>=2.90
gTTS>=2.3.2
pynput>=1.7.6

# Vision
Pillow==11.1.0
opencv-python==4.10.0.84
numpy==1.26.4

# Control
pyautogui>=0.9.54
# pywin32                    # Windows only — install with: pip install pywin32
playwright>=1.40.0

# Web & Search
requests==2.32.3
beautifulsoup4>=4.12.3
duckduckgo-search>=5.3.0
rank-bm25==0.2.2

# Memory
mem0ai>=0.1.0
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Documents (RAG)
pypdf>=4.0.0
python-docx>=1.1.2

# ADB & Networking
pure-python-adb>=0.3.0
# adb-wifi-py               # optional; install manually if ADB-WiFi required
qrcode[pil]>=7.4.2

# Interfaces
rich>=13.7.0
# pyqt6                      # optional GUI — not activated; install manually
# python-telegram-bot        # install separately for full async Telegram support
pystray>=0.19.5

# Tasks
APScheduler==3.10.4
SQLAlchemy==2.0.29

# Validation
pydantic==2.11.3

# Utils
python-dotenv==1.0.1
loguru>=0.7.2
psutil==5.9.8
pytest>=8.1.0,<9.0.0
huggingface_hub>=0.20.0,<1.0.0
